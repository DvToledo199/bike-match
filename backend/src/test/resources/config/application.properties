# Tests use their own database so they never change local development data.
# Create it once on an existing Docker volume: see "Pruebas" in the README.
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${POSTGRES_TEST_DB:bikematch_test}
